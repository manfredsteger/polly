The E2E test failures in your repository seem to have a recurring problem with **timeouts** and **database configuration**.

From the logs and test files analysis:
1. **Timeout Issues in E2E Tests**
   - Both the tests in [voting.spec.ts](https://github.com/manfredsteger/polly/blob/3404ed79ba6ca347160cd4aaa798d2a0e55bb3fe/e2e/voting.spec.ts) and [poll-creation.spec.ts](https://github.com/manfredsteger/polly/blob/3404ed79ba6ca347160cd4aaa798d2a0e55bb3fe/e2e/poll-creation.spec.ts) use `page.waitForURL()` with timeouts set to `30000ms` while waiting for `**/poll-success**`.

     **Root Cause**:
     - Delays in backend processing might cause the page to not load within the defined timeout.

     **Fix**:
     - Increase the timeout value in the tests:
       ```typescript
       await page.waitForURL('**/poll-success**', { timeout: 60000 });
       ```

2. **Database Role Issue**
   - Test logs repeatedly show `FATAL: role "root" does not exist`.
   - Your GitHub Actions workflow uses `POSTGRES_USER: test`, but it seems some part of the application or test is attempting to use `root` as the PostgreSQL user role.

     **Fix**:
     - Ensure the PostgreSQL user in the database URL matches the correct user defined in the workflow (`POSTGRES_USER: test`).
     - Check your application’s and test configuration files to ensure consistent `user:test`.

     Alternatively, you can create the `root` role in the database:
     ```sql
     CREATE ROLE root WITH SUPERUSER LOGIN PASSWORD 'password';
     ```

3. **Playwright Tests: Unhandled Dialogs or Errors**
   - Some tests wait for specific selectors and dialogs. Ensure the elements are present and visible by extending the `timeout` values or adding conditional checks.

---

### Workflow & Test Files Participating in E2E:
- **Test Files**:
  - [voting.spec.ts](https://github.com/manfredsteger/polly/blob/3404ed79ba6ca347160cd4aaa798d2a0e55bb3fe/e2e/voting.spec.ts)
  - [poll-creation.spec.ts](https://github.com/manfredsteger/polly/blob/3404ed79ba6ca347160cd4aaa798d2a0e55bb3fe/e2e/poll-creation.spec.ts)

- **Workflow**:
  - [ci.yml](https://github.com/manfredsteger/polly/blob/3404ed79ba6ca347160cd4aaa798d2a0e55bb3fe/.github/workflows/ci.yml)

Apply these changes and re-run the tests to verify fixes.